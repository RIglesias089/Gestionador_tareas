package org.model;

public class Tarea extends Evento{
    public Tarea(
            String id_evento,
            String nombre_evento,
            String descripcion
    ) {

        super(id_evento, nombre_evento, descripcion);
    }
}
